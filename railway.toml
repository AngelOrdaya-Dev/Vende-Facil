[build]
builder = "nixpacks"

[deploy]
releaseCommand = "php artisan config:cache && php artisan route:cache && php artisan view:cache && mkdir -p database && touch database/database.sqlite && php artisan migrate --force"
